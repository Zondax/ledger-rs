[workspace]
resolver = "2"
members = [
    "ledger-apdu",
    "ledger-transport",
    "ledger-transport-hid",
    "ledger-zondax-generic",
]

exclude = []

[profile.release]
opt-level = "s"
overflow-checks = true
