[workspace]
members = [
    "ledger-apdu",
    "ledger-transport",
    "ledger-transport-hid",
    "ledger-zondax-generic"
]

[profile.release]
# Tell `rustc` to optimize for small code size.
opt-level = "s"
